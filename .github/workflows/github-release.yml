# Generated by npm-dual-publish. Edit the template, not this file.
name: GitHub pre-release

on:
  push:
    tags:
      - 'gh-v*'

permissions:
  contents: write

jobs:
  release:
    uses: intisy/workflows/.github/workflows/github-release.yml@main
    with:
      tag_prefix: 'gh-'
      patch_ceiling: 20
