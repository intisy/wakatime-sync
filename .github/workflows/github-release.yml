# Generated by npm-dual-publish. Edit the template, not this file.
name: GitHub pre-release

on:
  workflow_dispatch:
    inputs:
      tag:
        description: 'Release tag to build and release; empty takes the ref the run was started from'
        required: false
        type: string
        default: ''
  push:
    tags:
      - 'gh-v*'

permissions:
  contents: write

jobs:
  release:
    uses: intisy/workflows/.github/workflows/github-release.yml@main
    with:
      tag_prefix: 'gh-'
      patch_ceiling: 20
      tag: ${{ inputs.tag }}
