{
  "name": "wakatime-sync",
  "version": "1.8.3",
  "description": "WakaTime plugin for OpenCode and Claude Code, automatically tracking time spent coding.",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "type": "module",
  "license": "MIT",
  "author": "intisy",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/intisy-ai/wakatime-sync.git"
  },
  "keywords": [
    "opencode",
    "claude",
    "wakatime",
    "plugin",
    "time",
    "tracking"
  ],
  "files": [
    "dist/",
    "README.md",
    "LICENSE"
  ],
  "engines": {
    "node": ">=20.0.0"
  },
  "scripts": {
    "build": "npm run build:deps && tsc --noEmit && node build.mjs",
    "postbuild": "node dist/index.js readme",
    "test": "npm run build && vitest run",
    "prepublishOnly": "npm run build",
    "build:deps": "npm --prefix core install && npm --prefix core run build"
  },
  "devDependencies": {
    "@opencode-ai/plugin": "^0.15.30",
    "@types/node": "^20.0.0",
    "esbuild": "^0.25.12",
    "typescript": "^5.4.0",
    "vitest": "^2.0.0"
  },
  "dependencies": {
    "@intisy-ai/core": "file:core"
  }
}
